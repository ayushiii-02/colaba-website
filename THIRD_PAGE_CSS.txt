/* =========================
   FEATURES SECTION
========================= */
.section-features {
  padding: 80px 0;
}

.features-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 48px;
}

/* =========================
   FEATURES HEADER
========================= */
.features-header {
  text-align: center;
}

.third-page-title {
  font-size: 48px;
  font-weight: 500;
  line-height: 57.6px;
  letter-spacing: -1.2px;
  color: #000000;
}

/* =========================
   FEATURES CARDS
========================= */
.features-cards {
  width: 100%;
  overflow: hidden;
}

.mySwiper {
  width: 100%;
  padding: 20px 0;
}

.mySwiper .swiper-wrapper {
  transition-timing-function: linear !important;
}

.mySwiper .swiper-slide {
  width: 280px;
  height: 320px;
  display: flex;
}

/* =========================
   FEATURE CARD
========================= */
.feature-card {
  width: 100%;
  height: 100%;
  border-radius: 24px;
  padding: 24px;
  background: #ffffff;
  box-shadow: 0px 12px 30px rgba(0, 0, 0, 0.12);
  display: flex;
  flex-direction: column;
}

/* =========================
   FEATURE ICON
========================= */
.feature-icon {
  width: 70px;
  height: 70px;
  padding: 16px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #EEF4FF;
  box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.12);
}

.feature-icon img {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

.icon-1 img { opacity: 0.4; }
.icon-2 img, .icon-3 img, .icon-4 img { opacity: 1; }

/* =========================
   FEATURE CONTENT
========================= */
.feature-content {
  margin-top: 20px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  flex: 1;
}

.feature-title {
  font-size: 20px;
  font-weight: 600;
  line-height: 28px;
  letter-spacing: -0.2px;
  color: #060606;
  margin: 0;
}

.feature-desc {
  font-size: 14px;
  font-weight: 400;
  line-height: 22px;
  color: #060606;
  opacity: 0.7;
  margin: 0;
}

.feature-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: #377DF6;
  font-size: 14px;
  font-weight: 600;
  line-height: 22px;
  text-decoration: none;
  margin-top: auto;
}

.feature-link:hover {
  text-decoration: underline;
}

.feature-link-icon {
  width: 16px;
  height: 16px;
  color: #377DF6;
  flex: 0 0 auto;
}

/* =========================
   RESPONSIVE
========================= */
@media (max-width: 768px) {
  .third-page-title {
    font-size: 32px;
    line-height: 1.3;
  }

  .mySwiper .swiper-slide {
    width: 250px;
    height: 300px;
  }
}
