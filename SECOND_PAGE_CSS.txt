/* =========================
   OUTREACH SECTION
========================= */
.section-outreach {
  padding: 80px 0;
}

.outreach-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 48px;
}

/* =========================
   OUTREACH HEADER
========================= */
.outreach-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  text-align: center;
}

.second-page-title {
  font-size: 48px;
  font-weight: 500;
  line-height: 57.8px;
  letter-spacing: -1.2px;
  color: #000000;
  max-width: 800px;
}

.second-page-subtext {
  font-size: 16px;
  line-height: 25.6px;
  letter-spacing: -0.2px;
  color: #060606;
  max-width: 605px;
}

/* =========================
   METRICS BAR
========================= */
.metrics-bar {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 60px;
  flex-wrap: wrap;
}

.metric {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
}

.metric-value {
  font-size: 36px;
  font-weight: 700;
  color: #060606;
  line-height: 1;
}

.metric-label {
  font-size: 14px;
  color: #3D3D3D;
  text-align: center;
}

.metric-divider {
  width: 1px;
  height: 48px;
  background-color: #3D3D3D;
  opacity: 0.3;
}

/* =========================
   CTA BUTTON
========================= */
.outreach-cta {
  display: flex;
  justify-content: center;
}

.cta-btn {
  background-color: #377DF6;
  color: #ffffff;
  padding: 14px 28px;
  border-radius: 12px;
  border: none;
  font-size: 16px;
  font-weight: 500;
  cursor: pointer;
  box-shadow: 0px 10px 20px rgba(55, 125, 246, 0.35), 0px 4px 8px rgba(0, 0, 0, 0.08);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.cta-btn:hover {
  transform: translateY(-4px);
  box-shadow: 0px 16px 30px rgba(55, 125, 246, 0.4), 0px 8px 12px rgba(0, 0, 0, 0.1);
}

/* =========================
   RESPONSIVE
========================= */
@media (max-width: 768px) {
  .second-page-title {
    font-size: 32px;
    line-height: 1.3;
  }

  .metrics-bar {
    gap: 30px;
  }

  .metric-value {
    font-size: 28px;
  }

  .metric-divider {
    display: none;
  }
}
